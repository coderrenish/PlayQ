

export const config = {
  timeout: "30000",
  patternConfig: "athenaaa",
  baseUrl: "https://your-app.com",
  featureFlags: {
    enableBetaUI: true,
    useMockBackend: false
  },
  supportedLanguages: ["en", "fr", "es"]
};